# Python dependencies for python/f1_data_fetcher.py
# Install: pip install -r requirements.txt  (Python 3.11+)
#
# Only these are imported directly. fastf1 pulls in matplotlib, requests,
# scipy and friends transitively, so they don't need to be listed here.

fastf1>=3.6.1
pandas>=2.0
numpy>=1.24
